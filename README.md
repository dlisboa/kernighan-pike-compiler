# Kernighan and Pike's compiler

Compiler from Chapter 8 of *The UNIX Programming Environment*.

Each **hocN** directory is one iteration of the compiler design. Run `make`
inside each dir to compile the binary.
